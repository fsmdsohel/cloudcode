export interface IFeatureCard {
  icon: any;
  title: string;
  description: string;
}

export interface IPricingCard {
  title: string;
  price: string;
  features: string[];
  highlight?: boolean;
}

export interface IWorkspace {
  id: string;
  name: string;
  createdAt: string;
  updatedAt: string;
}

export interface IWorkspace {
  id: string;
  name: string;
  createdAt: string;
  updatedAt: string;
}
